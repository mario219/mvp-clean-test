package com.example.domain.model

import com.example.domain.constants.ONE_DOUBLE

data class Rates(
    var eur: Double = ONE_DOUBLE,
    var aud: Double = ONE_DOUBLE,
    var bgn: Double = ONE_DOUBLE,
    var brl: Double = ONE_DOUBLE,
    var cad: Double = ONE_DOUBLE,
    var chf: Double = ONE_DOUBLE,
    var cny: Double = ONE_DOUBLE,
    var czk: Double = ONE_DOUBLE,
    var dkk: Double = ONE_DOUBLE,
    var gbp: Double = ONE_DOUBLE,
    var hkd: Double = ONE_DOUBLE,
    var hrk: Double = ONE_DOUBLE,
    var huf: Double = ONE_DOUBLE,
    var idr: Double = ONE_DOUBLE,
    var ils: Double = ONE_DOUBLE,
    var inr: Double = ONE_DOUBLE,
    var isk: Double = ONE_DOUBLE,
    var jpy: Double = ONE_DOUBLE,
    var krw: Double = ONE_DOUBLE,
    var mxn: Double = ONE_DOUBLE,
    var myr: Double = ONE_DOUBLE,
    var nok: Double = ONE_DOUBLE,
    var nzd: Double = ONE_DOUBLE,
    var php: Double = ONE_DOUBLE,
    var pln: Double = ONE_DOUBLE,
    var ron: Double = ONE_DOUBLE,
    var rub: Double = ONE_DOUBLE,
    var sek: Double = ONE_DOUBLE,
    var sgd: Double = ONE_DOUBLE,
    var thb: Double = ONE_DOUBLE,
    var tryy: Double = ONE_DOUBLE,
    var usd: Double = ONE_DOUBLE,
    var zar: Double = ONE_DOUBLE
)